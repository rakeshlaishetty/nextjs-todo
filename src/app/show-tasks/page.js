import ShowTasks from "./ShowTasks";


export const metadata = {
    title: "ShowTasks tasks",
    description: "ShowTasks tasks"
}

const ShowTasksPage = () => {

   
    return (
        <ShowTasks />
    )
}

export default ShowTasksPage